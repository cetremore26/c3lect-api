DATABASE_URL="postgresql://..."
JWT_SECRET="cambiar-por-valor-seguro"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"
PORT=3000
FRONTEND_URL="http://localhost:5173"
NODE_ENV="development"

RESEND_API_KEY=
RESEND_FROM=
ADMIN_EMAIL=

MP_ACCESS_TOKEN=
MP_WEBHOOK_SECRET=
API_URL=http://localhost:3000

# Meta Conversions API. Sin las dos primeras, el servicio se desactiva solo
# y no manda ningún evento (no falla, solo avisa por log).
META_PIXEL_ID=
META_CAPI_ACCESS_TOKEN=
# Solo mientras pruebas: hace que los eventos aparezcan en Events Manager →
# Test Events. BÓRRALA cuando termines de verificar.
META_CAPI_TEST_EVENT_CODE=
